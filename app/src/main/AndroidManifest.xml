<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".CampusBuddiesApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CampusBuddies"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Settings Activity -->
        <activity
            android:name=".presentation.screens.SettingsActivity"
            android:exported="false" />

        <!-- Search Activity -->
        <activity
            android:name=".presentation.screens.SearchActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:name=".presentation.screens.EditProfileScreen"
            android:exported="false" />

        <!-- Auth Activity -->
        <activity
            android:name=".presentation.screens.AuthActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize" />

        <!-- Splash Activity -->
        <activity
            android:name=".presentation.screens.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- OnBoarding Activity -->
        <activity
            android:name=".presentation.screens.OnboardingActivity"
            android:exported="false" />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>