<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CampusBuddies" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CampusBuddies" parent="Base.Theme.CampusBuddies" />

    <style name="ChipStyle" parent="Widget.MaterialComponents.Chip.Choice">
        <!-- Text -->
        <item name="android:textSize">@dimen/textSm</item>
        <item name="android:textColor">@color/chip_text</item>
        <item name="chipStartPadding">8dp</item>
        <item name="chipEndPadding">8dp</item>
        <item name="chipSpacingVertical">4dp</item>

        <item name="chipStrokeColor">@color/transparent</item>
        <item name="chipStrokeWidth">0dp</item>

        <item name="chipCornerRadius">16dp</item>

    </style>


    <style name="CircleImageView">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>


    <style name="RoundedSquareImageView">
        <item name="cornerSize">8dp</item>
    </style>

</resources>